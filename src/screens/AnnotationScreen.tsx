import React from 'react';
import { View, Text, Button } from 'react-native';

const AnnotationScreen = ({ navigation }) => {
  return (
    <View>
      <Text>Welcome to the Annosefa Screen!</Text>
    </View>
  );
};

export default AnnotationScreen;